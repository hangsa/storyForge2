# Creative Canvas v2 frontend flag — mirrors backend ENABLE_CANVAS_V2.
# The backend flag is authoritative: it controls whether the v2 router is
# mounted at backend/main.py:100-102. The frontend has no gate today; this
# entry exists so future dual-gated rollouts can flip both at once.
VITE_CANVAS_V2=false
